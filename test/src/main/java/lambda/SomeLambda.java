package lambda;

/**
 * Lambdas
 * In order to create Lambdas, we first need to create an interface
 * The functions inside the interface will be implemented by the lambda.
 * It should only contain a single method?
 * */
public interface SomeLambda {
    void printHello(String name);
}
